type Task {
  id: ID! @id
  name: String!
  description: String
  categoryId: ID!
  done: Boolean!
  doneDate: DateTime
  dueDate: DateTime!
}

type Project {
  id: ID! @id
  name: String!
  description: String
  color: String!
}