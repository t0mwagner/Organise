{
  "name": "todolist",
  "version": "0.1.0",
  "dependencies": {
    "apollo-link-context": "^1.0.20",
    "concurrently": "^5.3.0",
    "node": "^14.13.1"
  },
  "scripts": {
    "start": "concurrently \"yarn front\" \"yarn back\"",
    "front": "cd front && PORT=8000 yarn start",
    "back": "cd back && node ./src/index.js"
  }
}
